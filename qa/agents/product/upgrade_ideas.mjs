/* ============================================================
   אגף מוצר · רעיונות לשדרוג
   ------------------------------------------------------------
   הבדל מהותי משאר הסוכנים: כל סוכן אחר בודק *עובדה* אוטומטית
   (הקוד עשה X, המסך נטען, הסיסמה חשופה). רעיון לשדרוג הוא שיפוט —
   "מה יעזור למשתמשים שאין להם עדיין" — וזה דבר שסקריפט regex לא
   יכול להמציא. לכן הרשימה כאן **נכתבת ומתעדכנת בסקירה אנושית**,
   לא מיוצרת אוטומטית בכל ריצה. מתעדכנת כל פעם שיש עבודה מהותית על
   האפליקציה, כדי שהדוח היומי תמיד יכלול הצעות אמיתיות ולא רק ניקיון
   קוד — זו הייתה תלונה מוצדקת: הרעיון של כניסה עם Face ID לא היה
   יכול לצאת מסריקת קוד, והוא בדיוק סוג הרעיון שהיה חסר כאן.

   עודכן לאחרונה: 2026-08-04 — הרעיון הפתוח האחרון (renderReport/
   exportReport, קוד יתום) נסגר: הפונקציות נמחקו בסבב ניקוי קוד מת
   רחב יותר. אין כרגע רעיון פתוח ברשימה.
   ============================================================ */
export const IDEAS = [];

const agent = {
  id: 'product/upgrade-ideas',
  name: 'רעיונות לשדרוג',
  kind: 'human',
  domain: 'product',
  privacy: 'public',
  async run(){
    // shape שונה מממצא רגיל (sev="idea" במקום high/med/low/info) —
    // report.mjs מרנדר את התחום הזה בפורמט ייעודי, לא לפי חומרה.
    const findings = IDEAS.map(idea => ({ sev:"idea", title:idea.title, detail:idea.detail, effort:idea.effort }));
    return { summary:{ count: IDEAS.length }, findings };
  }
};
export default agent;

if(import.meta.url === `file://${process.argv[1]}`){
  const r = await agent.run();
  console.log(JSON.stringify(r, null, 2));
}
