# אבטחת מידע — טייסת 124

מסמך הפעלה מעשי. מסביר את מצב האבטחה, מה כבר בקוד, ומה צריך ללחוץ בקונסולת
Firebase כדי להפעיל אותו. השלבים כתובים לפי סדר — אפשר לעצור אחרי כל שלב.

## המצב שהיה (הבעיה)

כל נתוני האפליקציה (שמות, תאריכי לידה, הסמכות, תמונות חתימה) נשמרים ב-Firestore.
עד לשינוי הזה המסד היה **קריא ומחיק מהאינטרנט הפתוח, בלי שום התחברות** — הוכח
בפועל ששואבים את כל הנתונים ממחשב אקראי, בלי להקליד קוד ובלי לפתוח את האפליקציה.
הקודים שמקלידים בכניסה הם השוואת מחרוזת שרצה בדפדפן של המשתמש, ולכן מי שפותח את
קוד המקור עוקף אותם.

---

## שלב 1 — הפעלת אכיפת App Check (הכי דחוף, בלי קוד)

הקוד כבר מאתחל App Check עם reCAPTCHA v3. מה שחסר הוא **אכיפה** — בלעדיה
Firebase רק אוסף סטטיסטיקה ולא חוסם אף אחד.

1. Firebase Console → **App Check**.
2. לשונית **APIs** → שורת **Cloud Firestore**.
3. לפני ההדלקה — הסתכל בגרף: אם חלק ניכר מהבקשות מגיע **בלי** טוקן תקין (משתמשים
   אמיתיים שנכשלים ב-reCAPTCHA), הדלקה מיידית תנעל אותם. אם הגרף נקי — המשך.
4. לחץ **Enforce**.

בדיקה שזה תפס — הפקודה הבאה צריכה להחזיר **403** (לפני ההדלקה החזירה 200):

```
curl -s -o /dev/null -w "%{http_code}\n" \
  "https://firestore.googleapis.com/v1/projects/squadron124-96357/databases/(default)/documents/sq124?key=AIzaSyAEA2_BgNWhvZUvLBjdRVvW3epJgSHOZ6M"
```

---

## שלב 2 — פריסת כללי ה-Firestore

הקובץ `firestore.rules` שבמאגר מיישם: חובת אימות + חסימת שאיבה המונית (list)
+ חסימת מחיקה מבחוץ (delete).

הדרך הפשוטה (בלי CLI):
1. Firebase Console → **Firestore Database** → לשונית **Rules**.
2. הדבק את תוכן `firestore.rules`.
3. **Publish**.

או עם Firebase CLI:
```
firebase deploy --only firestore:rules
```

> אחרי שלב זה, קריאות ללא אימות ייכשלו. לכן חייבים לבצע את שלב 3 **באותו חלון זמן**,
> אחרת האפליקציה עצמה תיחסם. עדיף לבצע 3 קודם, לוודא שהאפליקציה עובדת, ואז לפרסם
> את הכללים.

---

## שלב 3 — הפעלת אימות אנונימי + הדלקת המצב בקוד

זהו "שלב א'" של האימות: אימות אנונימי שקוף למשתמש. יחד עם App Check (שלב 1) הוא
סוגר את הפרצה — האימות מוכיח "מי" (משתמש תקף), App Check מוכיח "מאיפה" (מהאתר
האמיתי בלבד). חוויית הכניסה של המשתמש **לא משתנה**.

1. Firebase Console → **Authentication** → **Sign-in method**.
2. הפעל את הספק **Anonymous**.
3. בקוד `index.html`, שנה שורה אחת:
   ```js
   const AUTH_MODE = "legacy";   →   const AUTH_MODE = "firebase";
   ```
4. פרוס, פתח את האפליקציה, ודא שהיא טוענת נתונים כרגיל (בקונסולת הדפדפן:
   "✅ אימות פעיל").
5. עכשיו בצע את שלב 2 (פרסום הכללים) וודא שהאפליקציה ממשיכה לעבוד.

אם משהו משתבש — החזרת `AUTH_MODE` ל-`"legacy"` מחזירה את ההתנהגות הקודמת מיד.

---

## שלב 4 (עתידי) — הפרדה מלאה בין מסגרות + PIN אישי

שלבים 1–3 סוגרים את הפרצה הקריטית (המסד כבר לא פתוח לעולם), אבל עדיין לא מפרידים
בין מסגרת למסגרת בצד השרת, ולא נותנים זהות אישית לכל משתמש. השלב הבא:

- **חשבון Firebase לכל מסגרת** (`shed3.sol@…`, `shed3.cmd@…`), כשהקוד שמקלידים
  הוא הסיסמה — כך הקוד יוצא מקוד המקור והופך לאישור שרת אמיתי.
- **החלפת בלוק הכללים** לגרסה 2 שכבר כתובה (בהערה בתוך `firestore.rules`):
  הפרדה בין מסגרות לפי כתובת המייל של החשבון המחובר.
- **הסדרת חשבונות-העל** (מנהל מערכת / קצין טכני / אחראי הדרכה) שרואים את כל
  הטייסת.
- **PIN אישי** כשכבה נוספת מעל זה.

נקודה שדורשת החלטה לפני שלב 4: **איפוס PIN/סיסמה של משתמש אחר על ידי המנהל דורש
Admin SDK** (Cloud Function), שזמין רק בתוכנית Blaze (בתשלום לפי שימוש, עם מכסה
חינמית נדיבה). יצירת חשבון וכניסה עצמית עובדות בתוכנית החינמית; רק איפוס-על-ידי-מנהל
דורש את השדרוג. מסך "ניהול משתמשים" באפליקציה בנוי כבר לשמש כמסך הזה.

---

## מה נשמר במסד — לשיקול צמצום

הפריט הרגיש ביותר הוא **תמונות החתימה** (נשמרות כ-base64). חתימה סרוקה היא נתון
אישי מובהק. שווה לשקול אם צריך את התמונה עצמה, או שמספיקה חותמת "נחתם ע"י X בתאריך Y".
מעבר לתמונות ל-Cloud Storage (במקום בתוך מסמכי Firestore) גם יקטין דרמטית את
מה שנחשף אם המסד דולף, וגם יפתור את מגבלת ה-1MB שהקוד נלחם בה היום.
